node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env.*.local
npm-debug.log*
*.tsbuildinfo
next-env.d.ts
.superpowers/
backups/

# Local tool config / editor state — not part of the deployed product
.claude/
.agents/
.obsidian/
.vscode/
.idea/
*.log

# OS files
.DS_Store
Thumbs.db

# Internal dev planning/session-log docs — useful as local context across sessions,
# not needed in the deployed product's repo.
CLAUDE.md
PLANO_EXECUCAO_V3.md
PROJECT_STATE.md

# Supabase CLI local cache/state (project ref, pooler host, component versions) —
# machine/session-specific, regenerated by `supabase link`, never a secret by itself
# but not something a shared repo should carry either.
supabase/.temp/

# Accidental duplicate folder from a copy-paste of the project directory
types - Copia/
